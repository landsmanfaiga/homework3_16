@using homework3_13Data
@model HomeViewModel 

@{
    ViewData["Title"] = "Home Page";
}

<html>
    <body>
<div b-gyvb0mk0dj class="container" style="margin-top:80px;">
        <main b-gyvb0mk0dj role="main" class="pb-3">
            
<div class="row">
    <div class="col-md-6 offset-md-3">
        @foreach(Ad a in Model.Ads)
                    {
                        <div class="bg-light p-4 rounded shadow mb-3">
                            <h5>Listed by @a.Title</h5>
                            <h5>Listed on @a.Date.ToShortDateString()</h5>
                            <h5>Phone Number: @a.PhoneNumber</h5>
                            <h3>Details:</h3>
                            <p>@a.Description</p>
                            @if(User.Identity.IsAuthenticated)
                            {
                                @if (Model.Account.Id == a.AccountId)
                                {
                                    <form method="post" action="/home/deletead">
                                        <input type="hidden" name="id" value="@a.Id" />
                                        <button class="btn btn-danger">Delete</button>
                                    </form>
                                }
                            }
                          
                        </div>
                    }
                   
    </div>
</div>
        </main>
    </div>
    
</body>
</html>